# Backend base URL (used by the Next.js rewrite proxy). The browser always
# calls same-origin /api/* which is proxied here.
BACKEND_URL=http://localhost:8000

# Public API base used by the client fetch helper (same-origin by default).
NEXT_PUBLIC_API_BASE=/api/v1
