"""Static configuration catalogs.

These are the source of truth for supported languages, voices, tones,
speaking styles, and capabilities. They are pure data (no I/O) so they can be
imported cheaply and preloaded into the in-process cache at startup.
"""
